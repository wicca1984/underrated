CRITICAL EXECUTION DIRECTIVE (read first): You are running headless in yolo mode. Do NOT stop to present a plan or wait for confirmation/feedback — there is no human to review it. Proceed DIRECTLY to implementing the code now. Only stop AFTER you have implemented the change, passed all gates, and committed. A previous attempt failed by stopping at the planning stage; do not repeat that.

You are a Gemini worker on `underrated` (an independent web browser engine in Rust, edition 2024). Work and respond in English. First, read the entire AGENTS.md passed via --include-directories (/workspaces/underrated-meta/AGENTS.md) and follow ALL of it (esp. I-1 self-implement boundary, I-3 no cross-worktree access, I-4 no skipped/ignored tests, I-5 one task = one cohesive concern, I-6 no unwrap/expect in src non-test code).

Work ONLY inside this worktree: /workspaces/wt/t0355 (branch agent/t0355-net-conn-pool). Language for all code/comments/commit messages: English.

## Task (t0355, milestone MS-NewTargets, parallel menu item 8): a host-keyed connection-reuse pool primitive in `src/net/`

Implement a MINIMAL, well-tested connection-reuse pool primitive that lets repeated HTTP fetches to the SAME origin (scheme+host+port) reuse a logical connection slot instead of treating every request as brand new. The goal is the basic data-structure + bookkeeping primitive (the basis for HTTP keep-alive / News-site multi-image fetching) — NOT a full TLS/socket connection manager.

### Hard scope
- Touch ONLY files under `src/net/` (you MAY add the pool struct to `src/net/mod.rs` or a new submodule file e.g. `src/net/pool.rs` plus its `mod pool;` line). Do NOT modify any other module — other workers own those.
- Do NOT duplicate or rewrite the existing `fetch_all_concurrent` helper added in t0349. Inspect what already exists in `src/net/mod.rs` first and build a SEPARATE, additive pool abstraction.
- Keep it pure/in-memory and synchronous-friendly: a struct (e.g. `ConnectionPool`) keyed by origin `(scheme, host, port)` that hands out / checks-in reusable connection slots with a bounded max-per-host, tracking idle vs in-use. No real sockets, no TLS, no async runtime requirement — model the lifecycle (acquire → release → reuse) as testable state. Leave actual socket binding as an explicit `// TODO(spec): bind real transport here`.
- Pure, additive behavior. Do NOT regress any existing test.

### Read first
Study `src/net/mod.rs`: the existing request/fetch types, how an origin/URL is represented, and the style of t0349's concurrency helper. Mirror the crate's error-handling conventions (Result types, no unwrap/expect in src).

### Tests (add a `#[cfg(test)] mod tests`)
Add at least:
1. Acquiring a slot for a new origin creates it; releasing it back marks it idle and a subsequent acquire for the same origin REUSES the same slot (assert reuse, not a new allocation).
2. Two different origins get independent slots.
3. The per-host bound is respected: acquiring beyond max-per-host either queues/denies per your documented policy (assert the bound holds).
Test code MAY use `unwrap()` (I-6 applies to src, not tests).

### Definition of done (all required before you commit)
1. `cargo fmt --all` clean.
2. `cargo clippy --all-targets --all-features -- -D warnings` clean.
3. `cargo test` green — your new tests AND every pre-existing test still pass.
4. `cargo doc --no-deps` clean.
5. `git diff --name-only origin/main...HEAD` shows ONLY files under `src/net/`.

### Commit
When all gates are green, COMMIT inside this worktree (do not leave changes uncommitted):
`git add -A && git commit -m "feat(net): host-keyed connection-reuse pool primitive (t0355)"`
Then STOP. Do not push; the orchestrator will open the PR.

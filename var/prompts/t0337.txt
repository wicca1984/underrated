You are a Gemini worker on `underrated` (an independent web browser engine in Rust, edition 2024). Work and respond in English.
First, read the entire AGENTS.md passed via --include-directories and follow all of it (esp. I-1..I-7). One task = one module/concern.

Task: t0337 (milestone MS-MVP-LAYOUT). Fix horizontal mis-centering of inline content inside a shrink-to-fit inline-block. This is a LAYOUT-ONLY task: the ONLY production file you may modify is `src/layout/mod.rs`.

THE BUG (root cause already isolated by the orchestrator — do NOT re-investigate, just fix):
In `layout_node` (src/layout/mod.rs), for a node whose children are all inline (`has_inline && !has_block` branch, ~line 211), the inline children are laid out via `layout_inline(..., content_width, ...)` BEFORE the shrink-to-fit width is known. `content_width` at that point is the available/outer width. AFTER the inline pass, `calculate_shrink_to_fit_width(...)` (~line 372) reduces `content_width` to the real shrunk content width for an auto-width inline-block. Result: `text-align` centering inside the inline pass was computed against the PRE-SHRINK (too large) width, so the centered child is shifted right by (available_width - shrunk_width)/2 and visually overflows the right edge of the now-smaller box.

CONCRETE REPRO (this is the real Google search box; verify your fix against it):
A 496px-wide input inside `display:inline-block` div under `text-align:center`. The input's LEFT edge lands at the inline-block's horizontal CENTER instead of its left edge. Expected: input centered == box left (zero free space, because a shrink-to-fit box hugs its content).

WHY normal blocks are fine: a normal block keeps its full (non-shrunk) width, so centering against `content_width` is already correct. ONLY the shrink-to-fit inline-block path is wrong, because `content_width` changes AFTER the inline pass.

THE FIX (Approach: re-layout inline content once the shrunk width is known — minimal, correct, handles multi-line):
1. In `layout_node`, the `if has_inline && !has_block { ... }` branch currently calls `layout_inline(...)` and pushes the resulting line boxes into `children`, advancing `child_cursor_y`. Keep this pass.
2. At the `if let Some(w) = calculate_shrink_to_fit_width(...) { content_width = w; }` site (~line 372): when shrink-to-fit returns a width `w` that DIFFERS from the `content_width` that was used for the inline pass, AND this node took the `has_inline && !has_block` branch, RE-RUN the inline layout pass against `w` and REPLACE the previously-pushed inline line boxes with the new ones. Concretely: remember (a) the `content_width` value used for the inline pass, (b) that the inline branch ran, and (c) the `child_cursor_y` at the start of the inline pass; if shrink-to-fit changed the width, clear the inline line boxes you pushed, call `layout_inline` again with the new `w` (same `block_container` node, same start x = `border_box_x + border_left + padding_left`, same start y, same depth, same `text_align`/`text_indent`/`word_spacing`), re-extend `children`, and reset `child_cursor_y` to the recomputed value. The second pass produces correctly-centered children because the centering free space is now (w - line_width)/2 (== 0 for the widest line, correct for narrower lines).
   - Implementation note: the cleanest structure is to capture the pre-pass `child_cursor_y` into a local (e.g. `inline_start_y`) and a flag `did_inline_pass`/`Option<f32> inline_pass_width`, then after `content_width = w` re-do the inline pass guarded by `if did_inline_pass && w != inline_pass_width`. Use `children.truncate(prev_len)` (record `children.len()` before the first inline extend) to drop exactly the inline boxes you added, NOT `children.clear()` (there could be earlier boxes in theory — be precise).
   - Do NOT change the `!has_inline` (all-block) branch or the MIXED branch. If you believe the MIXED (anonymous-block) branch has the same latent bug, DO NOT fix it here; leave a `// TODO(spec): same shrink-to-fit re-centering needed for mixed inline runs` comment and keep this task to the pure-inline branch only.
3. Do NOT alter `calculate_shrink_to_fit_width`'s logic, the centering formula in `src/layout/inline.rs`, or any other module. Layout-module-only, single concern.

SCOPE — STRICT, ONE MODULE. `git diff --name-only origin/main...HEAD` MUST list ONLY `src/layout/mod.rs` (plus any `var/` html/png you create). Do NOT modify, delete, weaken, rename, or `#[ignore]` ANY existing test anywhere. If you think you must touch another file, STOP, leave a `// TODO(spec):` note, and report the blocker.

REQUIRED NEW TEST (add to the existing `#[cfg(test)] mod tests` in `src/layout/mod.rs` — there are already many layout tests there; mirror their setup, e.g. `test_inline_block_shrink_to_fit`). The test must FAIL before your fix and PASS after:
- `test_shrink_to_fit_inline_block_centers_child_at_left_edge`: build a DOM/style equivalent to a `display:inline-block` container (auto width, no explicit width) under `text-align:center`, containing a single atomic inline child of a fixed width W (e.g. an inline-block child with `width: W`). Lay it out with a containing width much larger than W (e.g. 1000, W=200). Assert that the child's `rect.origin.x` equals the container box's left content edge (within 1.0px) — i.e. the child does NOT get shifted to the container's center. Equivalently assert `child.rect.origin.x - container_left < 1.0`. Reuse whatever DOM/style construction the surrounding tests use (read `test_inline_block_shrink_to_fit` and `test_form_control_button_intrinsic_width_and_height` for the exact pattern: building `Dom`, `HashMap<NodeId, ComputedStyle>`, and calling `layout_node`/the public layout entry).
   If matching the surrounding tests is easier via the snapshot API, you MAY instead drive it through `crate::oracle::export_snapshot(html, "", 1000, 400)` with html `<html><head><style>.b{display:inline-block;width:200px;height:20px}.w{text-align:center}</style></head><body><div class="w"><span class="b"></span></div></body></html>` and assert the `span`'s rect x equals the containing `div`'s content-left (NOT div_center). Pick ONE approach and make it green.

GATES (all must pass before commit):
- `cargo test` (entire suite) green — your new test AND all existing tests, ESPECIALLY `tests/oracle_snapshot_test.rs` (the `07_google_mock` fixture and all `test_inline_block_*` / `test_text_align_*` tests). If ANY existing test regresses, your fix is wrong — do not weaken the test, fix the approach.
- `cargo clippy --all-targets -- -D warnings` clean (NO `unwrap`/`expect` in non-test production code — I-6).
- `cargo fmt` then `cargo fmt --check` clean.

VERIFIED-IN-WINDOW (REQUIRED — this changes rendering geometry): after gates pass, `mkdir -p var` then create `var/t0337-stf-center.html` containing EXACTLY:
<html><head><style>.ds{display:inline-block;margin-left:4px}.lst{width:496px;height:25px;border:1px solid #888}.c{text-align:center}</style></head><body><div class="c"><div class="ds"><input class="lst" value=""></div></div></body></html>
then render the shipping-path PNG and SAVE it:
cargo run --example render_local_png -- /workspaces/wt/t0337/var/t0337-stf-center.html --width 1024 --height 120 --out /workspaces/wt/t0337/var/t0337-stf-center.png
The PNG MUST show the bordered input box HORIZONTALLY CENTERED in the 1024px viewport (its center near x=512), NOT shoved against the right edge. Report the saved PNG path. (The orchestrator will independently re-dump the real Google snapshot and re-verify the search input centers within ±4px before merge.)

COMMIT: after ALL gates pass AND the PNG is saved, `git add -A` then `git commit -m "fix(layout): re-center inline content against shrink-to-fit inline-block width (t0337)"`. Then run `git diff --name-only origin/main...HEAD` and confirm ONLY `src/layout/mod.rs` (plus var/ html+png) is listed. Do NOT push and do NOT open a PR — the orchestrator reviews, gates, and merges. Report a concise summary: the change, the new test, gate results, and the saved PNG path.

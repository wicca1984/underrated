You are a Gemini worker on `underrated` (an independent web browser engine in Rust, edition 2024). Work and respond in English. First, read the entire AGENTS.md passed via --include-directories (/workspaces/underrated-meta/AGENTS.md) and follow ALL of it (esp. I-1 self-implement boundary, I-3 no cross-worktree access, I-4 no skipped/ignored tests, I-5 one task = one cohesive concern, I-6 no unwrap/expect in src).

Work ONLY inside this worktree: /workspaces/wt/t0343 (branch agent/t0343-objfit-blit-fallback). Language for all code/comments/commit messages: English.

## Task (t0343, milestone MS-NewTargets): make the inline non-pre-decoded `<img>` blit fallback honor CSS `object-fit`.

### Background (verified by the orchestrator — trust these facts)
- `object-fit` (contain/cover/none/scale-down/fill) is ALREADY implemented and tested in `src/raster/mod.rs`
  for the `DisplayItem::Image` variant (it carries an `object_fit: crate::paint::ObjectFit` field). DO NOT
  touch raster — its math is correct and tested.
- The bug is ONLY in `src/paint/mod.rs`, in the `<img>` painting code. There are THREE emission sites:
  1. Pre-decoded path (`if let Some(pre_decoded) = dom.get_image(src)`, ~line 1313): emits
     `DisplayItem::Image { decoded: Some(..), object_fit: get_object_fit(style) }` — CORRECT, leave it.
  2. The `else` branch (non-pre-decoded, ~line 1321 onward): it tries to load+decode the image inline and,
     if successful, paints it as a grid of `DisplayItem::SolidRect`s by STRETCHING the image to fill the
     box (`sub_w = rect_w / decoded.width`, `sub_h = rect_h / decoded.height`). This stretch == CSS
     `object-fit: fill` and IGNORES any non-fill `object-fit`. This is the bug (see the existing
     `// TODO(spec):` comment there).
  3. The `if !painted_as_pixels { ... }` fallback (~line 1363): emits `DisplayItem::Image { decoded: None,
     object_fit: get_object_fit(style) }`, which raster then loads and renders WITH correct object-fit.
- `get_object_fit(style) -> ObjectFit` already exists in this file (~line 523). `ObjectFit::Fill` is the
  CSS default (returned when `object-fit` is absent or unrecognized).

### The fix (touch ONLY `src/paint/mod.rs`)
In the `else` (non-pre-decoded) branch, resolve `let obj_fit = get_object_fit(style);` once near the top of
the branch. Then GATE the inline SolidRect stretch fast-path so it only runs when `obj_fit ==
crate::paint::ObjectFit::Fill`. For any non-Fill value (Contain/Cover/None/ScaleDown), do NOT paint the
stretch grid — leave `painted_as_pixels = false` so control falls through to the existing
`if !painted_as_pixels { items.push(DisplayItem::Image { decoded: None, object_fit: get_object_fit(style) }) }`
branch, which routes to raster's correct object-fit rendering.

Concretely: wrap the existing `if let Some(bytes) = crate::loader::load_image_safely(...) && let Some(decoded)
= crate::image::decode_png(&bytes) { ... }` stretch block in `if obj_fit == crate::paint::ObjectFit::Fill {
... }`. Remove/replace the stale `// TODO(spec):` comment with a one-line comment explaining that non-fill
object-fit is delegated to the `DisplayItem::Image` path (raster). Keep the `painted_as_pixels` variable and
the Fill behavior byte-for-byte unchanged (full backward compatibility — default `fill` still stretches).

Do NOT change emission sites 1 or 3, raster, style, or the `DisplayItem` enum.

### Tests (REQUIRED — real assertions, never skipped/ignored — I-4)
Add `#[cfg(test)]` unit test(s) in `src/paint/mod.rs`. Mirror the existing paint tests (e.g.
`test_paint_visibility_hidden` ~line 3575) for the html→style→layout→display-list pipeline setup, and mirror
the raster object-fit tests (~line 1080) for generating a small NON-SQUARE valid PNG and writing it to a
unique temp file used as the `<img src=...>` (so `dom.get_image` returns None → the `else` branch runs and
`load_image_safely` can load the temp file). Clean up the temp file at the end of the test.

Cover at minimum:
1. `object-fit: contain` on a non-pre-decoded inline `<img>` whose box is non-square: assert the resulting
   display list CONTAINS a `DisplayItem::Image { object_fit: ObjectFit::Contain, decoded: None, .. }` for
   that image and does NOT contain the stretched SolidRect grid (e.g. assert there is no run of per-pixel
   SolidRects from the blit path — count SolidRects and assert it is far below decoded.width*decoded.height,
   or assert the Image variant is present while the blit grid is absent).
2. Regression guard: the default (no `object-fit`, i.e. `fill`) STILL takes the inline blit path — assert the
   display list contains SolidRect items from the stretch grid (and NOT a `DisplayItem::Image` for that img),
   proving backward compatibility is preserved.
Use a unique temp filename (include the test name) to avoid collisions; do not use `unwrap`/`expect` in `src`
non-test code (tests may use them).

### Done criteria (all required before you finish)
1. `cargo build` clean.
2. `cargo test` green (run the new tests; do not skip/ignore anything — I-4).
3. `cargo fmt --all` applied and `cargo clippy --all-targets -- -D warnings` clean.
4. `git diff --name-only origin/main` shows ONLY `src/paint/mod.rs`.
5. COMMIT your work in this worktree before finishing (do NOT leave changes uncommitted):
   git add -A && git commit with message:
   `fix(paint): honor object-fit on inline non-pre-decoded img blit fallback (t0343)`
   Then print the commit hash and `git diff --name-only origin/main`.

Report concisely in English: what you changed, test names added, and the final commit hash.

# Machine-enforce clippy restriction lints while allowing practical convenience in tests.
# unwrap/expect/panic in main code is forbidden by AGENTS.md I-6 -> denied via Cargo.toml [lints].
allow-unwrap-in-tests = true
allow-expect-in-tests = true
allow-panic-in-tests = true
